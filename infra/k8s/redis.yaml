apiVersion: v1
kind: Service
metadata:
  name: suptc-redis
  namespace: suptc
spec:
  selector:
    app: suptc-redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: suptc-redis
  namespace: suptc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: suptc-redis
  template:
    metadata:
      labels:
        app: suptc-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
