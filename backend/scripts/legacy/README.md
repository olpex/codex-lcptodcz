# Legacy patch scripts

This directory stores old one-off repository patch scripts that were previously kept at the repository root.

They are retained for traceability only. Do not use them as normal maintenance commands; prefer reviewed migrations,
typed CLI commands, or focused code changes with tests.
